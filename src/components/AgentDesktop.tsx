import './AgentDesktop.css'

function AgentDesktop() {
  return (
    <section className="desk" id="product">
      <div className="desk-inner">
        <h2 className="desk-title">How it works</h2>
        <p className="desk-lede">
          You start a session, an agent takes a run, you review what changed.
          Work stays in your org.
        </p>
      </div>
    </section>
  )
}

export default AgentDesktop
