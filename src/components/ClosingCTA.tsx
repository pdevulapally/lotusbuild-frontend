import './ClosingCTA.css'

function ClosingCTA() {
  return (
    <section className="closing" aria-labelledby="closing-heading">
      <div className="closing-inner">
        <h2 className="closing-title" id="closing-heading">See your next idea take shape.</h2>
        <p>Follow the agents, inspect the changes, and try the result.</p>
        <a href="#workspace-demo" className="hero-btn hero-btn-primary">Try the interactive demo</a>
      </div>
    </section>
  )
}

export default ClosingCTA
